function Home() {
  return (
    <section>
      {/* {news?.map((n) => (
        <div key={n.id}>n.title</div>
      ))} */}
      {/* {JSON.stringify(news)} */}
    </section>
  );
}

export default Home;
